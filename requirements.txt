numpy>=2.0.0,<2.4.0
opencv-python==4.10.0.84
onnxruntime==1.20.1
tqdm==4.66.5
requests==2.32.3

# Config / validation
# Keep this aligned with `inference` / `inference-models` constraints.
pydantic>=2.11.7,<2.12.0
pydantic-settings==2.4.0
python-dotenv==1.0.1

# API
fastapi==0.110.3
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Frontend
# NOTE:
# Streamlit can conflict with the newer Roboflow inference stack used by main.py.
# Install it separately only if you need streamlit_app.py.
# streamlit==1.38.0

# CV tooling
supervision==0.27.0.post2
inference==1.2.2
ultralytics>=8.4.37,<8.5.0
lap>=0.5.13,<0.6

# ML deps are resolved transitively by `inference` and `sports`.
# This explicit range avoids pip backtracking on newer `sports` installs.
transformers>=5.2.0,<5.3.0

git+https://github.com/roboflow/sports.git@main
